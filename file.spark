let double = fn(x) { return x*2; };
let input = [1,2,3];

let map = fn(f, lst) {
    if (x == []) {
        return x;
    }
    return [f( sub(lst, 0, 0) )] + map(f, sub(lst, 1, len(lst) - 1));
};

print(map(double, input));